# Build output
/dist/
/build/output/
*.tar.gz

# Python
__pycache__/
*.py[cod]
.venv
.pytest_cache/
.ruff_cache/

# Node
node_modules
.wrangler/

# Local environment, never committed
.env
.env.*
*.key
*.pem
credentials.json

# OS
.DS_Store

# SDD scratch workspace
.superpowers/

# Research data is the site's source; it is committed.

# Impeccable scratch
.impeccable/mocks/
.impeccable/direction-payload.json

# Build output
site/dist/

# Coverage artefacts. `.coverage` is a file and `htmlcov/` a directory,
# so both forms are needed; a bare `htmlcov` would not match the directory.
.coverage
.coverage.*
htmlcov/
