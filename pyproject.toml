[project]
name = "hostlaws-site"
version = "1.0.0"
description = "What hosting actually costs: a static comparison site"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.4",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
